# Copy this file to .env and set the real backend URL.
# Local dev: leave unset, it defaults to http://localhost:4000
# Production: set this to your deployed backend's URL, e.g.
# VITE_API_BASE_URL=https://your-backend.onrender.com
VITE_API_BASE_URL=
